using Cysharp.Threading.Tasks; # Assuming UniTask is used, otherwise Task

namespace {NAMESPACE}
{
    public interface I{SERVICE_NAME}
    {
        UniTask<bool> InitializeAsync();
        UniTask<{DATA_TYPE}> FetchDataAsync(string id);
    }
}
